# Version: niveau9/agent-api-completion-01
# Codex CLI: >= 0.142
# Copy this block into ~/.codex/config.toml and replace ${ZERO_MCP_HOST} with the
# deployed Zero origin (e.g. zero.example.dev). Never commit a real host+token.
# OAuth credentials are created interactively by:  codex mcp login zero
# This MCP server is DRAFT-ONLY: no send / permanent-delete / spam / account-settings tool.

[mcp_servers.zero]
url = "https://${ZERO_MCP_HOST}/mcp"
