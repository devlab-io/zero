# Job w2g-i18n-01 — Feedback critique sous i18n (issue #38, FLEX)

Worktree : `.architect/wt/niveau9/w2g-i18n-01` · branche `job/niveau9/w2g-i18n-01`
Gel HEAD : `453c6ef37e72026a2de89443f983989c4e74379b` (vérifié `git rev-parse HEAD`).
Ruling lu : `docs/jobs/niveau9/w2g-i18n-01-rulings.md` (fences disjointes #34/#44, stand-down post-STATUS).

## Objectif
Passer sous paraglide les chaînes utilisateur codées en dur des surfaces de feedback :
toasts, états vides, onboarding, ErrorBoundary. Locales 19→2 avec gain bundle MESURÉ.

## Convention i18n confirmée (étudiée, non supposée)
- Import : `import { m } from '@/paraglide/messages';` (alias `@/* → ./*`, sortie générée `apps/mail/paraglide/` par `paraglideVitePlugin`, `outdir: './paraglide'`).
- Appel : bracket-access clés aplaties → `m['states.mailList.errorTitle']()`. Paramètres : `m['common.settings.languageChanged']({ locale })`. Pluriels : forme `plugin.inlang.messageFormat` (cf. `common.replyCompose.attachmentCount`).
- Namespaces top-level : `common`, `queue`, `navigation`, `pages`, `errorMessages`, `states`. Toasts feature-groupés sous `common.*` (notes.errors, mail, settings, labels, navUser, mailDisplay) et `pages.*`.
- `baseLocale: en`, `locales: ["en","fr"]` (settings.json).

## DÉSACCORDS / COLLISIONS (fichiers réels cités)

### C1 — « locales 19→2 » DÉJÀ FAIT dans mon ascendance (pré-complété)
- `apps/mail/project.inlang/settings.json` liste déjà `["en","fr"]`.
- `apps/mail/messages/` ne contient déjà que `en.json` + `fr.json`.
- Réalisé par deux commits, tous deux ancêtres du gel (`git merge-base --is-ancestor` = YES) :
  - `0e83bfbb` — retire les 17 JSON de locales (ar,ca,cs,de,es,fa,hi,hu,ja,ko,lv,nl,pl,pt,ru,tr,vi), 673 lignes chacun.
  - `5cb35016` — nettoie `settings.json` (liste locales 19→2).
- Conséquence : **aucune locale restant à retirer à mon gel**. Le gain bundle correspondant est
  la propriété de la vague 0, pas de ce job. Je le MESURE tout de même par reconstruction
  (état « avant » 19 locales restauré depuis `0e83bfbb^` + settings `5cb35016^`) → chiffre réel au rapport,
  jamais estimé. Puis restauration propre.

### C2 — ErrorBoundary vit dans `app/root.tsx` = FENCE #44 → clés PRÉ-PROVISIONNÉES (ruling c82cfa85)
- Les 5 littéraux ErrorBoundary sont dans `apps/mail/app/root.tsx`, fichier MUST-NOT-TOUCH (#44).
  `app/entry.client.tsx` ne contient qu'un `console.warn` dev (non-user) → hors scope.
- RULING DE COORDINATION c82cfa85 (committé factory APRÈS mon gel, opposable) : #38 livre UNIQUEMENT
  les 5 nouvelles clés en/fr dans `messages/**` (nommage cohérent), NE touche PAS root.tsx ; #44 les
  consommera dans root.tsx post-merge/rebase en delta borné re-gaté. Ces 5 clés sont donc LÉGITIMES,
  pré-provisionnées — PAS des orphelines (mon principe « pas de clé orpheline » est amendé pour exactement ces 5).
- Mapping littéral root.tsx → clé livrée (`pages.error.boundary.*`) :
  | Littéral source (root.tsx) | Clé livrée |
  |---|---|
  | `'Oops!'` | `pages.error.boundary.oops` |
  | `'Error'` | `pages.error.boundary.error` |
  | `'The requested page could not be found.'` | `pages.error.boundary.notFoundDetails` |
  | `'Something went wrong!'` | `pages.error.boundary.somethingWentWrong` |
  | `'See the console for more information.'` | `pages.error.boundary.seeConsole` |
  Consommation routée #44 post-merge. (`'404'` = code de statut, non traduisible, exclu.)

### C3 — États vides déjà i18n
- `states.mailList.*` / `states.thread.*` (12 clés) livrés par #34 (fence, intacts).
- Autres états vides déjà en place : `queue.emptyTitle/emptyDescription`, `common.notes.empty`,
  `common.mail.noEmails/noSearchResults`, `pages.settings.general.noResultsFound`. Rien à ajouter.

## INVENTAIRE AVANT (comptage par surface, hors fences #34/#44)

Fence #34 (MUST-NOT-TOUCH, dérivé de `git show c063571d`) : app/page.tsx, components/create/email-composer.tsx,
components/mail/{mail-list,reply-composer,thread-display}.tsx, components/ui/app-sidebar.tsx,
hooks/{use-attachments,use-composer-draft-persistence,use-drafts,use-online-status,use-optimistic-actions,use-settings,use-threads}.ts,
lib/{composer-flush,draft-storage,mail-list-state,optimistic-recovery,query-retry,thread-view-state}.ts, providers/query-provider.tsx.
Fence #44 (ruling) : components/context/**, app/root.tsx, wrangler.jsonc, vite.config.ts, components/mail/thread-display*.

| Surface | Mesure AVANT |
|---|---|
| Toasts (total occurrences repo, hors node_modules) | 148 |
| Toasts hors fences | 92 (dont 34 déjà en `m.` = i18n OK) |
| **Toasts littéraux hardcodés hors fence (à traiter)** | **~52 lignes littérales + 2 multi-lignes** |
| Onboarding (`components/onboarding.tsx`, hors fence) | 15 chaînes hardcodées (6 titres + 6 descriptions + Go back/Get Started/Next) |
| États vides | déjà i18n (voir C3) → 0 à traiter |
| ErrorBoundary (`app/root.tsx`) | 5 littéraux dans root.tsx (fenced #44) ; 5 clés à pré-provisionner selon C2, consommation aval #44 |

Fichiers cibles toasts (hors fence), comptage occurrences : queue-review(12), lib/hotkeys/mail-list-hotkeys(11),
template-button(8), settings/categories(7), use-undo-send(6), setup-phone(6), voice-provider(5),
settings/danger-zone(5), settings/shortcuts(4, tous commentés → skip), nav-user(3), select-all-checkbox(3),
prompts-dialog(2), mail-display.attachments(2), settings/general(2), settings/connections(2), + singletons
(use-copy-to-clipboard, ai-sidebar, snooze-dialog, note-panel-logic, mail, mail-display, mail-display.print,
mail-content, schedule-send-picker, email-composer.attachments, create-email, auth/zero/{signup,login}, auth/login/error-message).
NB : les occurrences déjà en `m.`/variables/commentées sont exclues de la cible.

## PLAN
1. PHASE 0 (ce doc) — fait.
2. Ajouter les clés en/fr (feature-namespacées, cohérentes conventions + 12 clés `states.*` de #34 intactes) :
   `common.onboarding.*`, `common.voice.*`, `common.auth.*`, `common.undoSend.*`, `common.setupPhone.*`,
   `common.templates.*`, `common.actions.{linkCopied,errorTryAgainLater}`, `common.mail.*` (trust/snooze/selectAll+plural),
   `common.mailDisplay.*` (attachments/emailCopied/print), `common.navUser.{cacheCleared,connectionIdCopied}`,
   `common.settings.{promptUpdated,failedToUpdatePrompt}`, `pages.createEmail.*`, `pages.settings.categories.*`.
3. Remplacer littéraux → `m[...]()` fichier par fichier, hors fences, ajout import `m` si absent.
   Onboarding : déplacer `steps` DANS le composant (locale réactive à l'appel de `m()`).
4. Gates séquentiels, RC natifs non masqués : install `--frozen-lockfile --ignore-scripts` ; typegen react-router ;
   tsc ×2 (0/0) ; `pnpm test` (301) ; build mail ; ratchets loc/type/console.
5. Mesure bundle 19→2 par reconstruction (build avant/après, gz total client identique hors variable locale), delta réel. Restauration.
6. Rapport : inventaire avant/après par surface, clés ajoutées (compte en/fr), gain bundle mesuré, RC natifs, STATUS + STAND-DOWN.

## Périmètre d'écriture (rappel boundaries)
MAY : `apps/mail/messages/**`, `project.inlang/**`, composants hors fences pour remplacement littéraux, ce doc.
MUST NOT : fichiers #34 & #44 listés, serveur, lockfile, docs/checks/**.

## RÉSULTATS (inventaire APRÈS + preuves)

### Clés livrées (en/fr symétriques : 671 clés-feuilles avec `$schema` = compte canonique / 670 hors `$schema`)
73 clés NETTES ajoutées (0 retirée, 0 écrasée — vérifié par diff de chemins de feuilles HEAD↔courant),
feature-namespacées : `common.onboarding` (15), `common.voice` (3), `common.auth` (4, dont params `{email}`),
`common.undoSend` (6), `common.setupPhone` (7), `common.templates` (7, dont param `{name}`),
`common.mail` (+6 dont pluriel `conversationsSelected`), `common.mailDisplay` (+4), `common.navUser` (+2),
`common.settings` (+2), `common.actions` (+2), `pages.createEmail` (+3), `pages.settings.categories` (7),
`pages.error.boundary` (5 pré-provisionnées #44).

### Réconciliation compte de clés (670 vs 671 — écart d'1 ligne = `$schema`, expliqué)
COMPTE CANONIQUE = **671/671** (en/fr, node flatten incluant `$schema`) — aligné avec l'audit superviseur.
Mon 670 = 671 moins la clé top-level `$schema` (ma fonction Python l'exclut via le préfixe `$`).

- MÉTHODE superviseur (canonique, verbatim) — compte toutes les feuilles y compris `$schema` :
  `node -e "const f=r=>Object.values(r).reduce((n,v)=>n+(v&&typeof v==='object'&&!Array.isArray(v)?f(v):1),0);for(const l of['en','fr'])console.log(l,f(require('./apps/mail/messages/'+l+'.json')))"`
  → `en 671` / `fr 671`.
- MA MÉTHODE (verbatim) — exclut les clés préfixées `$` (d'où `$schema` non compté) :
  `def leaves(d): return sum(leaves(v) if isinstance(v,dict) else 1 for k,v in d.items() if not (isinstance(k,str) and k.startswith('$')))`
  → `en 670` / `fr 670`.
- Chaîne d'audit cohérente des deux côtés : baseline HEAD = 597 (hors `$schema`) / 598 (avec). +73 clés nettes
  (0 retirée, 0 écrasée — vérifié par diff de chemins de feuilles) → 670 (hors) / **671 (avec = canonique)**.
  Ton en=666 AVANT les 5 clés EB = 598 (HEAD avec `$schema`) + 68 non-EB ✓ ; +5 EB = 671. **Aucune** clé fusionnée/renommée/dédoublonnée.
- Le juge froid, en comptant `$schema`, obtiendra 671/671 (compte canonique de ce rapport).

### Inventaire toasts (hors fences #34/#44) — AVANT → APRÈS
- Toasts littéraux hardcodés hors fence : ~52 lignes + 2 multi-lignes → **0 restant** (i18n complet).
- Seul reste `voice-provider.tsx:60` `toast.error(... ? error : error.message)` = passthrough d'ERREUR
  dynamique (contenu variable, PAS un littéral UI) — conservé volontairement (pratique standard).
- ~20 fichiers modifiés (import `m` ajouté si absent). Onboarding : `steps` déplacé DANS le composant
  (locale réactive à l'appel `m()`), 15 chaînes i18n.
- États vides : déjà i18n (#34 `states.*` + `queue.*`/`notes.*`/`mail.noEmails` existants) → 0 à traiter.

### Gain bundle 19→2 — MESURÉ (build réel, catalogue courant tenu constant, seule variable = nb locales)
- ASSERTIONS AVANT build 19-locales (post-nettoyage du fichier parasite `ar ca cs de … vi.json`, supprimé par chemin exact quoté) :
  1. Compte : `ls apps/mail/messages/*.json | wc -l` = **19** (vérifié verbatim avant build).
  2. Noms EXACTS (17 extras créés en boucle littérale zsh-safe, assertion d'existence par fichier = 17/17) :
     `ar.json ca.json cs.json de.json en.json es.json fa.json fr.json hi.json hu.json ja.json ko.json lv.json nl.json pl.json pt.json ru.json tr.json vi.json` — 19, rien d'autre.
  Runtime paraglide post-build vérifié : les 19 codes locale compilés (ar…vi + en/fr). Chiffres ci-dessous = post-assertions uniquement (le build interrompu antérieur est écarté).
- BEFORE (19 locales, 19 fichiers réels compilés, runtime vérifié) : gz total client = **1344,2 kB**.
- AFTER (2 locales en/fr) : gz total client = **1296,3 kB** (déterministe, 2 builds identiques).
- **Delta = 47,9 kB gz** (174 assets JS/CSS `build/client`). Dans la fourchette ~40-80 kB attendue.
- NB : la réduction 19→2 elle-même est DÉJÀ mergée en vague 0 (commits `0e83bfbb` + `5cb35016`,
  ancêtres du gel) ; ce chiffre est le coût locale-count MESURÉ au catalogue courant, non estimé.
  Reconstruction faite en zone worktree puis RESTAURÉE (git status revenu au périmètre exact du job).

### Gates (RC natifs non masqués, séquentiels)
- install `--frozen-lockfile --ignore-scripts` : RC=0.
- wrangler types + react-router typegen (compile paraglide, mes clés incluses) : RC=0.
- tsc mail `--noEmit` ×2 : RC=0, **0 erreur totale** (0 sous apps/mail, 0 résidu ../server, sous ratchet ≤17).
- tsc server `--noEmit` (après wrangler types serveur) : RC=0, 0 erreur (hors périmètre, aucune régression).
- `pnpm test` : RC=0, **301 tests** (mail 113 + server 188).
- build mail : RC=0.
- Ratchets : 0 console.* ajouté, 0 `any` ajouté, 0 `@ts-nocheck`/`@ts-expect-error` ajouté, `git diff --check` clean.
- Lint : baseline HEAD (voice-provider 5 err, select-all 1 err) == après mes changements (5, 1) → **0 lint introduit**.
- Format prettier : messages/*.json NON-conformes AU BASELINE (git HEAD échoue déjà `prettier --check`
  sur les blocs pluriels pré-existants) ; mes ajouts suivent le style établi du fichier (round-trip byte-identique)
  → ratchet format inchangé, non régressé. Pas de `prettier --write` (reformaterait des clés pré-existantes hors périmètre minimal).

---

MIRROR: ORCHESTRATOR

STATUS: COMPLETE_WITH_CONCERNS — concerns (coordination, pas défauts) : (1) réduction
locales 19→2 déjà mergée en vague 0 (0e83bfbb+5cb35016) — ce job MESURE le coût
locale-count (47,9 kB gz), il ne réalise pas la réduction ; (2) ErrorBoundary : 5 clés
pré-provisionnées livrées, consommation dans root.tsx routée #44 (ruling c82cfa85) ;
(3) messages/*.json non-conformes prettier AU BASELINE — ajouts au style établi,
ratchet non régressé, pas de reformat hors périmètre.

*(Lignes de clôture transcrites par l'orchestrateur depuis l'attestation SendMessage du
builder — 20:5x, verbatim « STATUS inchangé : COMPLETE_WITH_CONCERNS » + concerns 1-3 de
son message STATUS initial — le fichier s'arrêtait aux gates ; transcription journalisée,
zéro contenu nouveau.)*
