# Fork notes → voir `../FORK.md`

Ce fichier a été fusionné dans le `FORK.md` racine (run pitbull, axe 8) : deux documents de fork
portant le même nom et divergeant l'un de l'autre valent moins qu'un seul document exact.

Y sont notamment conservés, corrigés et datés : la frontière **draft.only** de la surface MCP
externe (le `sendEmail` retiré, `createDraft` et `enqueueDraftJob` ajoutés), la politique de
cherry-pick upstream, et la vérification du risque d'envoi interne — qui, contrairement à ce que
ce fichier affirmait, n'existe plus dans l'inventaire des outils de l'agent.
